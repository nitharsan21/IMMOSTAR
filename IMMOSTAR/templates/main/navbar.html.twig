{% extends "base.html.twig" %}
{% block nav %}
    <style>
        
        
    </style>
    <div>
        
       <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
            <a class="navbar-brand" href="{{ path('accuiel') }}">IMMOSTAR<span class="sr-only">(current)</span></a>
            <div class="collapse navbar-collapse" id="navbarNavDropdown">
                <ul class="navbar-nav">
                   <div class="dropdown">
                  <a class="nav-link dropdown-toggle" data-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">Type</a> <!-- lien -->
                  <div class="dropdown-menu">
                    <a class="dropdown-item" href="{{ path('typeAfficher') }}">Liste</a>
                    <a class="dropdown-item" href="{{ path('typeCreate') }}">Creation</a>
                    <a class="dropdown-item" href="#">Dernier Service</a>
                  </div>
                </div>
                     <div class="dropdown">
                  <a class="nav-link dropdown-toggle" data-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">Bien</a> <!-- lien -->
                  <div class="dropdown-menu">
                    <a class="dropdown-item" href="{{ path('bienAfficher') }}">Liste</a>
                    <a class="dropdown-item" href="{{ path('bienCreate') }}">Creation</a>
                    <a class="dropdown-item" href="#">Trier par Type</a>
                  </div>
                </div>
                     <div class="dropdown">
                  <a class="nav-link dropdown-toggle" data-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">Visiteur</a> <!-- lien -->
                  <div class="dropdown-menu">
                   <a class="dropdown-item" href="{{ path('VisiteurAfficher') }}">Liste</a>
                    <a class="dropdown-item" href="{{ path('VisiteurCreate') }}">Creation</a>
                    <a class="dropdown-item" href="#">Dernier Service</a>
                  </div>
                </div>
                    
                </ul>
            </div>
            <div style="float: right;background-color: lightcoral;border-radius: 20%">
                <a class="navbar-brand" style="margin-left:5px;margin-right: 5px" href="{{ path('accuiel') }}">Déconnexion</a>
            </div>
        </nav>
            
    </div>
    {% endblock %}